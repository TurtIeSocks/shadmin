{
  "name": "AutocompleteInput",
  "props": [
    {
      "name": "limitChoicesToValue",
      "type": "boolean",
      "optional": true,
      "comment": "If true, the dropdown shows only the choice matching the current value instead of all choices"
    },
    {
      "name": "isOptionEqualToValue",
      "type": "(option: unknown, value: unknown) => boolean",
      "optional": true,
      "comment": "Custom equality check between a choice value and the current field value; defaults to areIdsEqual (loose ==)"
    },
    {
      "name": "handleHomeEndKeys",
      "type": "boolean",
      "optional": true,
      "comment": "If true, Home/End keys scroll the dropdown list to the first/last item"
    },
    {
      "name": "emptyValue",
      "type": "string | number",
      "optional": true,
      "comment": "The underlying form value stored when the selection is cleared. Defaults to empty string."
    },
    {
      "name": "emptyText",
      "type": "string",
      "optional": true,
      "comment": "Label of a '(none)' entry prepended when the field is not required, allowing users to clear the selection"
    },
    {
      "name": "clearOnBlur",
      "type": "boolean",
      "optional": true,
      "comment": "If true, clears the search text in the input when the input loses focus"
    }
  ]
}
