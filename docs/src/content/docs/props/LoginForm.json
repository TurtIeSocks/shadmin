{
  "name": "LoginForm",
  "props": [
    {
      "name": "className",
      "type": "string",
      "optional": true,
      "comment": "Extra class names applied to the `<Form>` element."
    },
    {
      "name": "redirectTo",
      "type": "string",
      "optional": true,
      "comment": "Path the user is redirected to after a successful sign in.\nDefaults to ra-core's default redirect (`/`)."
    }
  ]
}
