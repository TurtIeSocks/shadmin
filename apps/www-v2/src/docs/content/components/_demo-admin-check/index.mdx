---
title: "Demo Admin Check"
---

# Demo Admin Check

Confirms the DemoAdmin component is importable and the admin context compiles.

<ComponentPreview slug="components/_demo-admin-check" />

:::tip
The `DemoAdmin` wrapper is available at `@/docs/mdx/demo-admin`. The span below
renders via the static demo (no SSG-unsafe context calls). To use real ra-core
context, add `ra-core` to www-v2's dependencies and update `demo-admin.tsx`.
:::
