{
  "name": "DataTable",
  "props": [
    {
      "name": "body",
      "type": "ComponentType<{ children: ReactNode; rowClassName?: (record: RecordType, rowIndex: number) => string | undefined }>",
      "optional": true,
      "comment": "Override the table body component. Defaults to DataTableBody."
    },
    {
      "name": "bulkActionButtons",
      "type": "ReactNode",
      "optional": true,
      "comment": ""
    },
    {
      "name": "bulkActionsToolbar",
      "type": "ReactNode",
      "optional": true,
      "comment": ""
    },
    {
      "name": "children",
      "type": "ReactNode",
      "optional": false,
      "comment": ""
    },
    {
      "name": "className",
      "type": "string",
      "optional": true,
      "comment": ""
    },
    {
      "name": "expand",
      "type": "ReactElement<unknown, string | JSXElementConstructor<any>> | ComponentType<{ id: Identifier; record: RecordType; resource: string }>",
      "optional": true,
      "comment": "Element or function component rendered as the expand panel under each\nrow. When a function component, it receives `{id, record, resource}`.\nWhen provided, a chevron toggle column is added at the start of every row."
    },
    {
      "name": "expandSingle",
      "type": "boolean",
      "optional": true,
      "comment": "When true, expanding a row collapses any previously-expanded one so\nonly a single row is expanded at a time. Defaults to `false`."
    },
    {
      "name": "head",
      "type": "ComponentType<{ children: ReactNode }>",
      "optional": true,
      "comment": "Override the table head component. Defaults to DataTableHead."
    },
    {
      "name": "hover",
      "type": "boolean",
      "optional": true,
      "comment": "Set to false to disable hover highlighting on rows. Defaults to true."
    },
    {
      "name": "isRowExpandable",
      "type": "(record: RecordType) => boolean",
      "optional": true,
      "comment": "Predicate that decides whether a given record can be expanded. When it\nreturns `false`, the chevron toggle is hidden for that row."
    },
    {
      "name": "rowClassName",
      "type": "(record: RecordType, rowIndex: number) => string | undefined",
      "optional": true,
      "comment": ""
    },
    {
      "name": "rowClick",
      "type": "string | false | RowClickFunctionBase<RecordType> | (record: RecordType, id: Identifier) => string | false | Promise<string | false>",
      "optional": true,
      "comment": "Override the rowClick action. Accepts \"edit\", \"show\", \"expand\",\n\"toggleSelection\", false, a path string, or a function returning any of\nthose.\n\nFunction variants:\n- `(id, resource, record) => string | false` — standard ra-core form\n- `(record, id) => string | false` — convenience 2-arg form where\n  record is passed as the first argument\n\nString shortcuts: `\"expand\"` toggles the row expand panel;\n`\"toggleSelection\"` toggles row checkbox selection."
    },
    {
      "name": "size",
      "type": "DataTableSize",
      "optional": true,
      "comment": "Row density. \"small\" uses tighter cell padding; \"medium\" (default) uses\nstandard padding."
    }
  ]
}
