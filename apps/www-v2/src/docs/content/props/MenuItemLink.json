{
  "name": "MenuItemLink",
  "props": [
    {
      "name": "className",
      "type": "string",
      "optional": true,
      "comment": "Extra CSS class appended to the underlying `<SidebarMenuButton>`."
    },
    {
      "name": "icon",
      "type": "ReactNode",
      "optional": true,
      "comment": "Icon rendered before `label`. Pass a `lucide-react` icon (or any\nReactNode) sized to match other sidebar items."
    },
    {
      "name": "keyboardShortcut",
      "type": "string",
      "optional": true,
      "comment": "Keyboard shortcut string (e.g. `\"mod+k\"`, `\"shift+/\"`, `\"meta+s\"`).\nWhen pressed anywhere on the page, navigates to `to`.\n`mod` normalizes to `metaKey` on macOS, `ctrlKey` elsewhere."
    },
    {
      "name": "keyboardShortcutRepresentation",
      "type": "ReactNode",
      "optional": true,
      "comment": "Visual representation of the keyboard shortcut rendered after the label.\nDefaults to `<KeyboardShortcut keyboardShortcut={keyboardShortcut} />`."
    },
    {
      "name": "label",
      "type": "ReactNode",
      "optional": false,
      "comment": "The text rendered alongside `icon`. Strings are auto-translated via\n`useTranslate` (with the string itself as the fallback). Pass a ReactNode\nto render custom content without translation lookup."
    },
    {
      "name": "onClick",
      "type": "() => void",
      "optional": true,
      "comment": "Click handler invoked after the default navigation. The default\n`<AppSidebar>` uses this to close the mobile drawer."
    },
    {
      "name": "to",
      "type": "string | { hash?: string; pathname: string; search?: string; state?: unknown }",
      "optional": false,
      "comment": "Target path for the link. Compared against the current location to\nderive the active state. Accepts a string or a react-router `To` object."
    },
    {
      "name": "tooltipProps",
      "type": "Omit<ComponentProps<typeof Tooltip>, \"children\">",
      "optional": true,
      "comment": "Extra props forwarded to the `<Tooltip>` wrapping the menu item.\nApplies to both collapsed-sidebar and expanded-sidebar (if tooltipProps\nis provided) modes."
    }
  ]
}
