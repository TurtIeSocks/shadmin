{
  "name": "DashboardMenuItem",
  "props": [
    {
      "name": "className",
      "type": "string",
      "optional": true,
      "comment": "Extra CSS class appended to the underlying menu button."
    },
    {
      "name": "icon",
      "type": "ReactNode",
      "optional": true,
      "comment": "Replaces the default `<House />` icon. Pass any ReactNode."
    },
    {
      "name": "label",
      "type": "ReactNode",
      "optional": true,
      "comment": "Replaces the default translated label (`ra.page.dashboard`). Strings are\npassed through `useTranslate` with the string itself as fallback; ReactNodes\nare rendered as-is."
    },
    {
      "name": "onClick",
      "type": "() => void",
      "optional": true,
      "comment": "Click handler invoked after the default navigation. Useful to close the\nmobile drawer or trigger side effects."
    }
  ]
}
