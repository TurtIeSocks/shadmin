{
  "name": "AppSidebar",
  "props": [
    {
      "name": "appBarAlwaysOn",
      "type": "boolean",
      "optional": true,
      "comment": "When `true`, signals that the AppBar is always visible (not hidden on scroll).\nDescendants can read this flag via context or props to adjust their top spacing.\nThe flag is accepted here so it can be forwarded to the sidebar tree without\nrequiring a separate context provider in the layout."
    },
    {
      "name": "children",
      "type": "ReactNode",
      "optional": true,
      "comment": "Replaces the default `<Menu />` rendered inside the sidebar content area.\nPass a custom navigation tree when you need full control over sidebar items."
    },
    {
      "name": "closedSize",
      "type": "number",
      "optional": true,
      "comment": "Width of the sidebar in its collapsed/icon-only state, in pixels. Defaults to 55.\nApplied as an inline `style` override on the underlying `<Sidebar>` element."
    },
    {
      "name": "size",
      "type": "number",
      "optional": true,
      "comment": "Width of the sidebar in its open/expanded state, in pixels. Defaults to 240.\nApplied as an inline `style` override on the underlying `<Sidebar>` element."
    }
  ]
}
