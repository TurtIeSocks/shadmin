{
  "name": "CheckForApplicationUpdate",
  "props": [
    {
      "name": "disabled",
      "type": "boolean",
      "optional": true,
      "comment": "When true, the polling is skipped."
    },
    {
      "name": "fetchOptions",
      "type": "RequestInit",
      "optional": true,
      "comment": "Options forwarded to the internal `fetch()` call — allows custom headers,\ncredentials, abort signal, etc. The `cache` option is always overridden to\n`\"no-store\"` so change detection remains reliable."
    },
    {
      "name": "interval",
      "type": "number",
      "optional": true,
      "comment": "Interval in milliseconds between two checks. Defaults to 3600000 (1 hour)."
    },
    {
      "name": "notification",
      "type": "ReactNode",
      "optional": true,
      "comment": "Notification rendered when a new version is detected.\nDefaults to `<ApplicationUpdatedNotification />`."
    },
    {
      "name": "onNewVersionAvailable",
      "type": "() => void",
      "optional": true,
      "comment": "Called when a new version is detected (hash change), in addition to\nrendering the `notification`. Useful for side effects such as analytics or\nauto-reloading after a grace period."
    },
    {
      "name": "url",
      "type": "string",
      "optional": true,
      "comment": "URL to download to check for code update. Defaults to\n`window.location.origin + \"/index.html\"`."
    }
  ]
}
