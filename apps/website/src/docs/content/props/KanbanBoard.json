{
  "name": "KanbanBoard",
  "props": [
    {
      "name": "cardRenderer",
      "type": "(record: R) => ReactNode",
      "optional": true,
      "comment": "Fully custom card renderer. Receives the raw record and must return a\nReactNode. When provided, `titleSource` and `descriptionSource` are\nignored."
    },
    {
      "name": "columns",
      "type": "KanbanColumnDef[]",
      "optional": false,
      "comment": "Ordered list of column definitions. Records whose `groupBy` value matches\n`column.id` appear in that column. Unmatched records are silently ignored."
    },
    {
      "name": "descriptionSource",
      "type": "string",
      "optional": true,
      "comment": "Optional field rendered as a secondary line on each card."
    },
    {
      "name": "groupBy",
      "type": "string",
      "optional": false,
      "comment": "Field name whose value buckets records into columns.\ne.g. `\"status\"`, `\"stage\"`, `\"priority\"`."
    },
    {
      "name": "onCardClick",
      "type": "(record: R) => void",
      "optional": true,
      "comment": "Called when a card is clicked. Defaults to navigating to the record's\nshow page (`/<resource>/<id>/show`)."
    },
    {
      "name": "titleSource",
      "type": "string",
      "optional": true,
      "comment": "Field used as the card title. Falls back to `recordRepresentation` when\nomitted."
    }
  ]
}
