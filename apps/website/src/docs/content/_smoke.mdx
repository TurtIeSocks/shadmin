---
title: Smoke Test
---

# MDX Pipeline Smoke Test

This page exercises all 5 constructs the pipeline must support.

## 1. Callout

:::note
This is a **note** callout rendered via `remark-directive` → `<Callout>`.
:::

:::tip
Tip callouts work too.
:::

## 2. Tabs

<Tabs>
  <TabItem label="React">
    ```tsx title="hello.tsx"
    export function Hello() {
      return <p>Hello world</p>;
    }
    ```
  </TabItem>
  <TabItem label="TypeScript">
    ```ts
    const greeting: string = "hello world";
    ```
  </TabItem>
</Tabs>

## 3. Fenced Code Block with Title

```tsx title="example.tsx"
import { Button } from "shadmin";

export default function Example() {
  return <Button variant="default">Click me</Button>;
}
```

## 4. Relative Link

See also [Install](./install) and the [InspectorButton section](#5-propstable).

## 5. PropsTable

<PropsTable name="InspectorButton" />
