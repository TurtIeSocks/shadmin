import { ComponentDirectory } from "./component-directory";

/** /docs/components — component directory, linking to doc pages. */
export default function DocsComponents() {
  return <ComponentDirectory basePath="/docs" />;
}
