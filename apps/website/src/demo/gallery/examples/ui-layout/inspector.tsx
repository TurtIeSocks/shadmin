export default function Example() {
  return (
    <p className="text-sm text-muted-foreground">
      Inspector renders inside the Admin layout when preferences-editor mode is
      enabled.
    </p>
  );
}
