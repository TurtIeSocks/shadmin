export default function Example() {
  return (
    <p className="text-sm text-muted-foreground">
      ReferenceError is rendered inside a ReferenceField when the referenced
      record cannot be loaded.
    </p>
  );
}
