export default function Example() {
  return (
    <p className="text-sm text-muted-foreground">
      LoginPage is the full authentication screen — rendered by the Admin router
      when unauthenticated.
    </p>
  );
}
