export default function Example() {
  return (
    <p className="text-sm text-muted-foreground">
      Layout wraps the entire admin application — rendered inside the Admin
      component.
    </p>
  );
}
