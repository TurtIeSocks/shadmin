{
  "name": "Title",
  "props": [
    {
      "name": "className",
      "type": "string",
      "optional": true,
      "comment": "Optional CSS class applied to the rendered title element."
    },
    {
      "name": "defaultTitle",
      "type": "string | ReactElement",
      "optional": true,
      "comment": "Fallback title used when `title` is undefined.\nTranslated if a string."
    },
    {
      "name": "title",
      "type": "string | ReactElement",
      "optional": true,
      "comment": "The title to display. If a string, it is translated using the i18n provider.\nPass a React element to render a custom title."
    }
  ]
}
